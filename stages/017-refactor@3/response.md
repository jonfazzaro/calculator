I hoisted the `readOperands` helper and `operators` lookup table out of `read()` in `src/index.ts:27`. Previously they were rebuilt on every recursive call to `read`; now they're constructed once per `evaluateSpokenExpression` call, and `read` itself is shorter and only concerned with dispatching on the current token. Behavior is unchanged — same recursion order, same closures over `fail`/`place`/`pieces`.