Extracted the tokenizing logic (lowercasing, parenthesis spacing, splitting, and filtering) into a named `tokenize` function, separating "turn text into words" from "parse and evaluate words" within `evaluateSpokenExpression`. This is a pure extraction with no behavior change—same regex, same order of operations, just given an intention-revealing name and pulled out of the larger function.