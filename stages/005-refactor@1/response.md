Collapsed the four near-identical operator branches (`add`, `subtract`, `multiply`, `divide`) into a single `OPERATORS` lookup table pairing each word with its connector and combine logic, then one shared parsing path in `read()`. This removes the duplicated "read → expect connector → read → combine" pattern while preserving exact behavior (same tokens, same error cases, same divide-by-zero check).