Replaced four near-duplicate `if (word === …)` branches (add/subtract/multiply/divide) with a single `operators` lookup table mapping each operator word to its connector and arithmetic operation. This removes the repeated "read operands, apply operation" shape and makes adding future operators a data change rather than a new branch, while preserving exact behavior (including divide-by-zero failure).